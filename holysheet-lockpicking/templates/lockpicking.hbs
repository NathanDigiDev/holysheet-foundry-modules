{{!--
  Gabarit unique de la PART "main" de l'ApplicationV2.
  Le rendu du verrou lui-même est fait en Canvas 2D (élément <canvas>),
  le reste (HUD) est du HTML statique alimenté par _prepareContext().
--}}
<div class="hslp-lp">

  {{!-- Surface de jeu : tout le dessin (barillet, crochet, clé de tension) y est peint en JS --}}
  <canvas class="hslp-lp__canvas" width="420" height="420"></canvas>

  {{!-- Tête haute : crochets restants + statut courant --}}
  <div class="hslp-lp__hud">
    <div class="hslp-lp__picks" title="{{localize 'HSLP.Picks'}}">
      <i class="fa-solid fa-screwdriver-wrench"></i>
      <span class="hslp-lp__pick-count" data-pick-count>{{picks}}</span>
    </div>
    <div class="hslp-lp__status" data-status>{{statusText}}</div>
  </div>

  {{!-- Aide contextuelle --}}
  <p class="hslp-lp__help">{{localize "HSLP.Help"}}</p>
</div>
